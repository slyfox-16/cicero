# 05 - Expose OpenClaw via Tailscale Serve (Tailnet-Only HTTPS)

This guide moved to:

- `ops/saturn/tailscale-serve.md`

Rationale: Treat Serve mappings as host ops (not Clawbot-specific) so future Cicero services can share the same pattern.
