#!/usr/bin/env bash
set -euo pipefail

# Prints the absolute path to the repo root.
#
# This script assumes it lives in: <repo>/bin/cicero-root

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(cd -- "${script_dir}/.." && pwd)"

printf '%s\n' "${repo_root}"

