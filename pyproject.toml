[project]
name = "cicero"
version = "0.1.0"
description = "Cicero — personal AI assistant MCP servers and tooling"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "chromadb>=0.6.0",
    "mcp>=1.0.0",
    "sentence-transformers>=3.0.0",
]

[tool.uv]
dev-dependencies = []
